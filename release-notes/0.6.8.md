# 0.6.8

## Changed

- **A new icon.** The waveform now stands on a longer first bar that carries below the others, so
  the mark reads as a lowercase "p" — a proper sibling to TermTile's "T" rather than just a
  relative. The menu bar shows the same mark, and still inverts while PushText is listening.
